# ChatFold Environment Configuration
# 复制此文件为 .env 或 .env.local 并修改配置

# ==================== 环境配置 ====================
# 环境: local-dev | test | production
CHATFOLD_ENVIRONMENT=local-dev

# 调试模式
CHATFOLD_DEBUG=true

# ==================== 数据库配置 ====================
# 数据库类型: sqlite | mysql
# - sqlite: 本地开发，快速启动，无需 Docker 容器
# - mysql: 生产环境，需要 Docker 或远程数据库
CHATFOLD_DATABASE_TYPE=sqlite

# 可选：显式指定数据库 URL（会覆盖自动生成）
# SQLite: sqlite:///path/to/database.db
# MySQL: mysql+pymysql://user:pass@localhost:3306/chatfold
# CHATFOLD_DATABASE_URL=

# MySQL 配置（仅当 DATABASE_TYPE=mysql 时需要）
# CHATFOLD_MYSQL_HOST=localhost
# CHATFOLD_MYSQL_PORT=3306
# CHATFOLD_MYSQL_USER=chatfold
# CHATFOLD_MYSQL_PASSWORD=chatfold_dev
# CHATFOLD_MYSQL_DATABASE=chatfold

# ==================== Redis 配置 ====================
# Redis 类型: fake | docker
# - fake: 内存模拟 Redis，无需 Docker 容器（推荐用于本地开发）
# - docker: 真实 Redis，需要 Docker 容器或远程服务
CHATFOLD_REDIS_TYPE=fake

# Redis 连接配置（仅当 REDIS_TYPE=docker 时需要）
CHATFOLD_REDIS_HOST=localhost
CHATFOLD_REDIS_PORT=6379
CHATFOLD_REDIS_INDEX=0

# ==================== 存储模式 ====================
# 内存模式（开发/测试用）
# true: 所有数据存储在内存，重启丢失
# false: 数据持久化到数据库
CHATFOLD_USE_MEMORY_STORE=false

# ==================== 外部服务配置 ====================
# NanoCC API (蛋白质折叠编排服务)
# CHATFOLD_NANOCC_URL=
# CHATFOLD_NANOCC_API_KEY=

# Folding GPU API (GPU 推理服务)
# CHATFOLD_FOLDING_GPU_URL=
# CHATFOLD_FOLDING_GPU_API_KEY=

# ==================== 快速开发模式 ====================
# 零依赖模式（无需 Docker，最快启动）：
# CHATFOLD_DATABASE_TYPE=sqlite
# CHATFOLD_REDIS_TYPE=fake
# CHATFOLD_USE_MEMORY_STORE=true  # 注意：数据不会持久化
# 启动命令: uv run uvicorn app.main:app --reload

# 持久化开发模式（无需 Docker，数据保留）：
# CHATFOLD_DATABASE_TYPE=sqlite
# CHATFOLD_REDIS_TYPE=fake
# CHATFOLD_USE_MEMORY_STORE=false
# 启动命令: uv run uvicorn app.main:app --reload

# 完整生产模拟模式（需要 Docker 容器）：
# CHATFOLD_DATABASE_TYPE=mysql
# CHATFOLD_REDIS_TYPE=docker
# CHATFOLD_USE_MEMORY_STORE=false
# 需先运行: ./scripts/local-dev/start.sh
# 启动命令: uv run uvicorn app.main:app --reload
