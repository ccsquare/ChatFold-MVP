# ChatFold-MVP Production Environment Configuration
# This file contains production environment defaults

# =============================================================================
# PRODUCTION PORTS (标准端口，与Docker/K8s一致)
# =============================================================================

# Frontend production port
FRONTEND_PORT=3000

# Backend production port
BACKEND_PORT=8000

# Production environment
NODE_ENV=production

# =============================================================================
# PRODUCTION URLS (将被部署时覆盖)
# =============================================================================

# Production URLs - these should be overridden by deployment environment
# FRONTEND_URL=https://chatfold.com
# NEXT_PUBLIC_BACKEND_URL=https://api.chatfold.com
# NEXT_PUBLIC_API_URL=https://api.chatfold.com/api/v1

# Local production testing URLs
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# =============================================================================
# BACKEND PRODUCTION CONFIGURATION
# =============================================================================

# Backend server configuration
PORT=8000
HOST=0.0.0.0
DEBUG=false

# Frontend configuration for CORS
FRONTEND_PORT=3000
FRONTEND_HOST=localhost

# Production database (should be overridden)
DATABASE_TYPE=mysql
REDIS_TYPE=redis
USE_MEMORY_STORE=false

# Production features
USE_MOCK_NANOCC=false
ENVIRONMENT=production

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# In actual production deployment, override these via:
# 1. Kubernetes ConfigMap/Secrets
# 2. Docker environment variables
# 3. CI/CD environment variables
# 4. Cloud provider configuration systems
#
# Critical production overrides:
# - NEXT_PUBLIC_BACKEND_URL: Actual backend domain
# - CHATFOLD_DATABASE_URL: Production database connection
# - CHATFOLD_REDIS_HOST/CHATFOLD_REDIS_PORT: Production Redis