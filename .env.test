# ChatFold-MVP Test Environment Configuration
# This file contains test environment overrides

# =============================================================================
# TEST ENVIRONMENT PORTS
# =============================================================================

# Test environment uses standard ports for Playwright and CI/CD
FRONTEND_PORT=3000
BACKEND_PORT=8000

# Test environment
NODE_ENV=test

# =============================================================================
# TEST URLS
# =============================================================================

# Test URLs (used by Playwright and integration tests)
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# =============================================================================
# BACKEND TEST CONFIGURATION
# =============================================================================

# Backend server configuration for testing
CHATFOLD_PORT=8000
CHATFOLD_HOST=0.0.0.0
CHATFOLD_DEBUG=true

# Frontend configuration for CORS
CHATFOLD_FRONTEND_PORT=3000
CHATFOLD_FRONTEND_HOST=localhost

# Test database (isolated and fast)
CHATFOLD_DATABASE_TYPE=sqlite
CHATFOLD_REDIS_TYPE=fake
CHATFOLD_USE_MEMORY_STORE=true

# Test features
CHATFOLD_USE_MOCK_NANOCC=true
CHATFOLD_ENVIRONMENT=test

# NanoCC filesystem root (local test)
NANOCC_FS_ROOT=/SPXvePFS/mewtool-test/sessions

# =============================================================================
# TEST-SPECIFIC CONFIGURATION
# =============================================================================
# - Uses memory store for speed and isolation
# - Uses fake Redis to avoid Docker dependency
# - Uses SQLite for test database isolation
# - Enables mock NanoCC for predictable test results
# - Standard ports for compatibility with existing test configurations