# ChatFold-MVP Development Environment Configuration
# This file contains overrides for local development environment

# =============================================================================
# DEVELOPMENT PORTS (使用标准端口)
# =============================================================================

# Frontend development server port
FRONTEND_PORT=3000

# Backend development server port
BACKEND_PORT=8000

# Development environment
NODE_ENV=development

# =============================================================================
# DEVELOPMENT URLS
# =============================================================================

# Frontend application URL
FRONTEND_URL=http://localhost:3000

# Backend API base URL (for frontend SSE and direct API calls)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Backend API endpoint URL (for auth and REST API calls)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# =============================================================================
# BACKEND DEVELOPMENT CONFIGURATION
# =============================================================================

# Backend server configuration
PORT=8000
HOST=0.0.0.0
DEBUG=true

# Frontend configuration for CORS auto-generation
FRONTEND_PORT=3000
FRONTEND_HOST=localhost

# Development database (no Docker required)
DATABASE_TYPE=sqlite
REDIS_TYPE=fake
USE_MEMORY_STORE=false

# Development features
USE_MOCK_NANOCC=true
ENVIRONMENT=local-dev

# NanoCC filesystem root (local development)
NANOCC_FS_ROOT=/SPXvePFS/mewtool-test/sessions

# =============================================================================
# DEVELOPMENT CORS ORIGINS (Auto-generated by backend)
# =============================================================================
# The backend will auto-generate CORS origins based on CHATFOLD_FRONTEND_* settings:
# - http://localhost:3000
# - http://127.0.0.1:3000
# This replaces hardcoded CHATFOLD_CORS_ORIGINS